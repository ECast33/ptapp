#using git from cloud9
Add files and commit them 
```sh 
git add . 
```
commit the files and changes 
```sh 
git commit -m "First commit"
```
push the changes 
```sh 
git push -u origin master
```
pull from git 
```sh 
git pull
```